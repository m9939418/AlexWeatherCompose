package com.alex.yang.weather.demo.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.SpanStyle
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp

val Typography = Typography(
    bodyLarge = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 16.sp,
        lineHeight = 24.sp,
        letterSpacing = 0.5.sp
    )
)

// 80 w500
val Brand80Medium = TextStyle(
    fontWeight = FontWeight.Medium,
    fontSize = 80.sp,
    lineHeight = (80.sp * 1.4f),
    letterSpacing = 0.sp
)

// 70 w500
val Brand70Medium = TextStyle(
    fontWeight = FontWeight.Medium,
    fontSize = 70.sp,
    lineHeight = (70.sp * 1.4f),
    letterSpacing = 0.sp
)

// 50 w700
val Brand50Bold = TextStyle(
    fontWeight = FontWeight.Bold,
    fontSize = 50.sp,
    lineHeight = (50.sp * 1.4f),
    letterSpacing = 0.sp
)

// 50 w500
val Brand50Medium = TextStyle(
    fontWeight = FontWeight.Medium,
    fontSize = 50.sp,
    lineHeight = (50.sp * 1.4f),
    letterSpacing = 0.sp
)

// 45 w500
val Brand45Medium = TextStyle(
    fontWeight = FontWeight.Medium,
    fontSize = 45.sp,
    lineHeight = (45.sp * 1.4f),
    letterSpacing = 0.sp
)

// 24 w500
val Brand24Medium = TextStyle(
    fontWeight = FontWeight.Medium,
    fontSize = 24.sp,
    lineHeight = (24.sp * 1.4f),
    letterSpacing = 0.sp
)


// 20 w700
val Brand20Bold = TextStyle(
    fontWeight = FontWeight.Bold,
    fontSize = 20.sp,
    lineHeight = (20.sp * 1.4f),
    letterSpacing = 0.sp
)

// 20 w500
val Brand20Medium = TextStyle(
    fontWeight = FontWeight.Medium,
    fontSize = 20.sp,
    lineHeight = (20.sp * 1.4f),
    letterSpacing = 0.sp
)

// 18 w700
val Brand18Bold = TextStyle(
    fontWeight = FontWeight.Bold,
    fontSize = 18.sp,
    lineHeight = (18.sp * 1.4f),
    letterSpacing = 0.sp
)

// 18 w500
val Brand18Medium = TextStyle(
    fontWeight = FontWeight.Medium,
    fontSize = 18.sp,
    lineHeight = (18.sp * 1.4f),
    letterSpacing = 0.sp
)

// 16 w700
val Brand16Bold = TextStyle(
    fontWeight = FontWeight.Bold,
    fontSize = 16.sp,
    lineHeight = (16.sp * 1.4f),
    letterSpacing = 0.sp
)

// 16 w500
val Brand16Medium = TextStyle(
    fontWeight = FontWeight.Medium,
    fontSize = 16.sp,
    lineHeight = (16.sp * 1.4f),
    letterSpacing = 0.sp
)

// 14 w700
val Brand14Bold = TextStyle(
    fontWeight = FontWeight.Bold,
    fontSize = 14.sp,
    lineHeight = (14.sp * 1.4f),
    letterSpacing = 0.sp
)

// 14 w500
val Brand14Medium = TextStyle(
    fontWeight = FontWeight.Medium,
    fontSize = 14.sp,
    lineHeight = (14.sp * 1.4f),
    letterSpacing = 0.sp
)

// 12 w700
val Brand12Bold = TextStyle(
    fontWeight = FontWeight.Bold,
    fontSize = 12.sp,
    lineHeight = (12.sp * 1.4f),
    letterSpacing = 0.sp
)

// 12 w500
val Brand12Medium = TextStyle(
    fontWeight = FontWeight.Medium,
    fontSize = 12.sp,
    lineHeight = (12.sp * 1.4f),
    letterSpacing = 0.sp
)


val Span50Medium = SpanStyle(
    fontSize = 50.sp,
    fontWeight = FontWeight.Medium,
)

val Span45Medium = SpanStyle(
    fontSize = 45.sp,
    fontWeight = FontWeight.Medium,
)

val Span20Medium = SpanStyle(
    fontSize = 20.sp,
    fontWeight = FontWeight.Medium,
)